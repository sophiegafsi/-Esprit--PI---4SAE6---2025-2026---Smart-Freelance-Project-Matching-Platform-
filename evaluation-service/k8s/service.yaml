apiVersion: v1
kind: Service
metadata:
  name: evaluation-service
  namespace: dev
spec:
  selector:
    app: evaluation-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8095
  type: ClusterIP
