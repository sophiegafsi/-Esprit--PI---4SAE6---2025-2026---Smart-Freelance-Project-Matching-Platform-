apiVersion: v1
kind: Service
metadata:
  name: API-Gateway
  namespace: dev
spec:
  selector:
    app: API-Gateway
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8081
  type: ClusterIP
