apiVersion: v1
kind: Service
metadata:
  name: reservation-service
  namespace: dev
spec:
  selector:
    app: reservation-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8088
  type: ClusterIP
