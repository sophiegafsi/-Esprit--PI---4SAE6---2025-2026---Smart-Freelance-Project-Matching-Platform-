pipeline {
    agent any

    environment {
        SONAR_PROJECT_KEY = 'evaluation-service'
    }

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Tests unitaires + couverture') {
            steps {
                dir('evaluation-service/evaluation-service') {
                    sh 'chmod +x ./mvnw && ./mvnw clean test'
                }
            }
            post {
                always {
                    junit testResults: 'evaluation-service/evaluation-service/target/surefire-reports/*.xml', allowEmptyResults: true
                    archiveArtifacts artifacts: 'evaluation-service/evaluation-service/target/site/jacoco/jacoco.xml', allowEmptyArchive: true
                }
            }
        }

        stage('Build backend') {
            steps {
                dir('evaluation-service/evaluation-service') {
                    sh './mvnw -DskipTests package'
                }
            }
            post {
                success {
                    archiveArtifacts artifacts: 'evaluation-service/evaluation-service/target/*.jar', fingerprint: true
                }
            }
        }

        stage('SonarQube') {
            steps {
                dir('evaluation-service/evaluation-service') {
                    withSonarQubeEnv('SonarQube') {
                        sh './mvnw sonar:sonar -Dsonar.projectKey=$SONAR_PROJECT_KEY'
                    }
                }
            }
        }

        stage('Quality Gate') {
            steps {
                timeout(time: 5, unit: 'MINUTES') {
                    waitForQualityGate abortPipeline: true
                }
            }
        }
    }
}
