apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway
  namespace: freelink
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api-gateway
  template:
    metadata:
      labels:
        app: api-gateway
    spec:
      containers:
      - name: api-gateway
        image: dalirom123/api-gateway:latest
        ports:
        - containerPort: 8081
---
apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: freelink
spec:
  type: NodePort
  selector:
    app: api-gateway
  ports:
    - port: 8081
      targetPort: 8081
      nodePort: 30081
